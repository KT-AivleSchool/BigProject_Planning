# 🗺️ 지능형 다목적 스마트시티 입지 선정 플랫폼 'OmniSite' 조장 단독 진행 개발 로드맵

본 문서는 조장(PM) 단독 진행 트랙에 맞추어 의존성 순서에 따른 시스템 개발 액션 플랜과 각 파트별 체크리스트를 정리한 문서입니다.

---

## 📅 1단계: RDB / GIS / Vector 통합 물리 DB 구축 (의존성: 최상)
*   **목적:** 공간 데이터 기하 연산 및 RAG 조례 적재를 위한 단일 PostgreSQL 데이터베이스 뼈대 완성
*   **체크리스트:**
    *   [x] PostgreSQL 컨테이너 구동 및 외부 포트 포워딩(`5432`).
    *   [x] PostGIS 확장 활성화 (`CREATE EXTENSION IF NOT EXISTS postgis;`).
    *   [x] pgvector 확장 활성화 (`CREATE EXTENSION IF NOT EXISTS vector;`).
    *   [x] 물리 ERD 명세의 17개 마스터/통계/RAG 테이블 스키마 DDL 실행.
    *   [x] `geom` 공간 데이터 컬럼에 공간 인덱스(GIST) 선언 완료 확인.
    *   [x] `district_regulations` 테이블에 `district_id` 복합 인덱스(`idx_regulations_district_vector`) 선언 확인.

## 📅 2단계: 백엔드 파일 업로드 & AI 감리 / HITL API 구축 (Step 1~2)
*   **목적:** 데이터 적재 관문 마련 및 결측 좌표/지번에 대한 수동 보정 인터페이스 연동 API 구축
*   **체크리스트:**
    *   [x] FastAPI `Multipart/form-data` 일괄 수집 라우터 개발.
    *   [x] 수집된 조례 규정 및 파일 스키마에 대한 AI 감리 모의 분석 결과 반환 API.
    *   [x] 임시 파싱된 GeoJSON 및 속성 필드 전송 기능.
    *   [x] 위경도 결측값 검증 함수(`isValidCoordinate`) 구현.
    *   [x] Step 2 비주얼 HITL 좌표/지번 최종 보정 확정(`Commit`) API 구현.
    *   [x] 조례 PDF 독립 모달 업로드 분리 및 다중 업로드(`multiple`) 지원.
    *   [x] 조례 목록 비동기 조회 API 및 팝업 모달 UI 구현.
    *   [x] 조례 중복 업로드 차단 로직(Duplication Guard) 적용.
    *   [x] 조례 물리 파일 및 RAG 텍스트 캐시 동시 삭제 수거(Deletion Engine) 연동.

## 📅 3단계: AHP 일관성 검증 & PostGIS 차집합 입지 연산 API 구축 (Step 3~4)
*   **목적:** 의사결정 타당성 락(Lock) 체계 및 물리적 버퍼 배제 구역 연산 로직 완비
*   **체크리스트:**
    *   [ ] AHP 가중치 입력 행렬에 따른 일관성 비율(C.R. < 0.1) 수학적 검증 로직 구현.
    *   [ ] 검증 통과 시 `ahp_models` 테이블에 가중치 락(`is_locked = TRUE`) 저장 API 구현.
    *   [ ] PostGIS `ST_Difference` 및 `ST_Union`을 사용한 규제구역(지하철 10m, 스쿨존 200m 등) 공간 차집합 차단 쿼리 작성 및 튜닝 (GIST 인덱스 스캔 확인).
    *   [ ] 후보지 배후 통계(민원, 연령 등) 가중 합산 갈등 민감도 점수($CSS(p)$) 산식 구현.
    *   [ ] Top 1~3 부지 분기 UX 데이터 모델 규격 반환 API 구현.

## 📅 4단계: pgvector RAG & LangGraph SSE 토론 API 구축 (Step 5)
*   **목적:** RAG 기반 LLM 법규 순수 추론 에이전트 구축 및 SSE 실시간 스트리밍 완성
*   **체크리스트:**
    *   [ ] 담당자 관할지 코드(`district_id`) 선제 필터링(`Pre-filtering`) 기반 pgvector 코사인 유사도 RAG 조회 쿼리 구현.
    *   [ ] 찬성(상인), 반대(주민), 조정(공무원) 에이전트 간의 3턴 LangGraph 토론 루프 프롬프트 설계 (GPT-4o 연동).
    *   [ ] 토론 로그 텍스트를 `text/event-stream` 프로토콜로 한 자씩 클라이언트에 전달하는 SSE 스트리밍 API 구현.

## 📅 5단계: WeasyPrint PDF 및 Audit AI 피드백 루프 API 구축
*   **목적:** 행정 타당성 보고서 발급 제어 및 실제 사례 검증 환류 구조 구축
*   **체크리스트:**
    *   [ ] 도로점용료 산식이 바인딩된 WeasyPrint 기반 HTML-to-PDF 컴파일러 및 백엔드 발급 라우터 개발.
    *   [ ] 실제 완료 공문서 PDF 수신 및 OCR 텍스트 추출 라우터 개발.
    *   [ ] Audit AI 팩트체크 및 기존 3대 예측 시나리오(일반/우호/불합의) 1:1 매핑 알고리즘 구현.
    *   [ ] 검증된 데이터를 RAG 환류 오염 방지 격리 세그먼트에 보관하는 CRUD API 구현.

## 📅 6단계: Next.js - FastAPI 실물 데이터 API 연동 및 GIS 복원
*   **목적:** 프런트엔드 와이어 프레임과 실물 백엔드 연동 E2E 시나리오 최종 정합
*   **체크리스트:**
    *   [ ] [page.js](file:///c:/Users/Admin/Desktop/빅프로젝트 관련자료/최종1차/1.0-prototype/frontend/src/app/page.js) 내의 하드코딩된 모크 상태값들을 백엔드 API 연동(Axios/Fetch)으로 교체.
    *   [ ] EventSource를 통한 AI 실시간 토론 SSE 로그 스트리밍 바인딩.
    *   [ ] 동결된 Leaflet GIS 코드 영역(싱글톤 맵 초기화 및 마커 스로틀링) 무결성을 유지한 채, 백엔드로부터 전송받은 실 지적도 및 Top 1~3 마커 데이터 렌더링 동기화.
