# 📍 OmniSite 조장 단독 진행 개발 체크포인트 (CHECKPOINT)

본 문서는 조장(PM) 단독 진행 트랙의 현재 개발 상태와 후속 조치 사항을 정리해 놓은 표시목입니다. 에이전트가 재기동될 때 이 문서의 맥락을 로드하여 일관된 개발 방향성을 유지합니다.

---

## 📊 현재 진행 상황 (Status)

*   **1단계: RDB / GIS / Vector 통합 DB 구축 완료 [x]**
    *   Docker Compose 기반 로컬 DB 기동 성공 (`omnisite-db` 컨테이너 백그라운드 구동 완료)
    *   PostgreSQL 15 + PostGIS 3.3.4 + pgvector 0.4.4 확장 모듈 빌드 및 연동 완료.
    *   [01_schema.sql](file:///c:/Users/Admin/Desktop/빅프로젝트%20관련자료/최종1차/1.0-prototype/DB/init/01_schema.sql)에 기반한 18개 테이블 스키마 및 공간 GIST 인덱스 적재 완료.
*   **2단계: 백엔드 파일 업로드, AI 감리 & RAG 관리 기능 고도화 완료 [x]**
    *   **로그인/인증 시스템 모킹 처리**:
        *   공무원 회원가입 및 JWT 로그인, 패스워드 암호화 API/DB 규격은 2차 EKS 다중 테넌트 이관 단계로 전환하고, 현재 프로토타입 v2.3.0 단계에서는 프론트엔드 인증 모킹(자치구 및 ID 입력 폼)으로 완결 대체 완료.
    *   **공간 데이터 (CSV) 다중 업로드 및 AI 감리 연동**:
        *   FastAPI 백엔드에서 대용량 데이터 OOM 방지를 위한 경량 헤더 파서(`parse_csv_header`) 적용 완료.
        *   파일명과 컬럼 정보만을 기반으로 OpenAI API 교차 감리 및 도메인 목적 시맨틱 추론 구현 완료.
    *   **⚖️ 법규 RAG 관리 모달 및 조례 독립 업로드**:
        *   기존 메인 Left Panel에서 혼재되어 있던 조례 PDF 등록 창구를 글로벌 네비게이션 헤더 모달로 완전 분리 완료.
        *   여러 개의 PDF 조례 파일을 한 번에 Ingestion할 수 있도록 다중 업로드(`multiple`) 지원.
    *   **📋 조례 목록 조회, 중복 검증 및 삭제 기능 구현 (v2.3.0) [x]**:
        *   **조례 목록**: 글로벌 헤더에 단추를 신설하여 비동기 fetch를 통해 등록된 조례(파일명 및 용량) 목록 팝업 제공.
        *   **중복 업로드 방지 (Duplication Guard)**: 업로드 시 동일 파일명 존재 분기를 백엔드에 두어 `400 Bad Request` 에러를 통한 원천 차단 및 프론트 피드백 적용.
        *   **물리적 삭제 및 캐시 수거 (Deletion Engine)**: `DELETE /api/v1/upload/regulations/{filename}` API를 통해 저장소 내 원천 파일 삭제 및 추출 텍스트 캐시 파일(`filename.txt`) 동시 소거 처리.
*   **3단계: AHP 가중치 가변 연산 및 공간 연산 연동 초안 구축 완료 [x]**
    *   일관성 비율(C.R. < 0.1) 실시간 연산 및 UI 락킹 조건 연동 성공.

---

## ❄️ 동결 코드 사양 (Code Freeze Rules)

후속 개발 단계에서 어떠한 형상 변경도 불허하는 핵심 GIS 무결성 로직입니다.
- **Leaflet GIS 초기화 및 메모리 관리**:
  - `page.js` 내부의 비동기 싱글톤 스크립트 로드, 맵 초기화 옵션(`L.map`), Ref 캐시 해제용 명시적 `.enable()` API 호출 구문.
- **마커 드래그 스로틀링 및 금역 예외처리**:
  - 마커 드래그 시 `marker.isWarning` 상태 전환 필터링 및 지하철역(30m)/학교(200m)/군사구역(400m) 침범 시 좌표 복원 및 경고창 호출 로직.

---

## 🚀 3~4단계 이행할 핵심 포인트 (Next Action Points)

1.  **PostGIS 다기준 공간 차집합 연산의 실질적 백엔드 연동**
    *   사용자가 AHP 가중치 확정 및 추천 입지 연산 단추를 누를 때, 실제 데이터베이스 상의 지적 테이블(`cadastral_lands`)에 `ST_Difference` 및 `ST_Buffer` 공간 쿼리를 날려 Top 1~3 필지 리스트를 동적 반환받도록 보강.
2.  **지역 갈등 민감도 벡터 ($CSS(p)$) 연산 수식 결합**
    *   추천된 후보지 PNU 코드를 기반으로 주변 소상공인 상권(`commercial_shops`), 불법 민원(`civil_complaints`), 연령대 주민등록인구(`age_demographics`) 통계를 조인 집계하여 갈등도 점수와 등급[상/중/하]을 연산해 에이전트 통제 파라미터로 바인딩.
3.  **GPT-4o 기반 LangGraph 모의 심의 토론 SSE 스트리밍 연동**
    *   RAG 조례 데이터와 CSS 벡터를 OpenAI LLM에 공급하여 찬반 주민대표와 공무원 에이전트 간의 3자 토론 스크립트를 EventSource를 통해 실시간 스트리밍 타이핑 UI로 뿌려주는 파이프라인 개발.
